const styles = theme => ({
  card: {
    minWidth: 300,
    maxWidth: 300
  },

  title: {
    marginBottom: 16,
    fontSize: 14,
    color: theme.palette.text.secondary
  }
});

export default styles;
